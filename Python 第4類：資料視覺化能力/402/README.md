## 作答提要
 - 基本上，答案都在提示提及了。
 - 讀寫檔案，先檔名路徑，後讀取模式（僅讀取可略）。
 - 參數 label 表示該數據在圖例中顯示的名稱。
   
## 特別注意
 - 方法 x_ticks 表示水平軸實際刻度值，供引進的數據對應座標用。方法 xlabel 則表示水平軸顯示刻度值，兩者長度彼此對應。
 > 是的！實際刻度值與要顯示的刻度值可以不同。

 > 垂直軸也相同，但名稱內的 x 要改寫成 y。