## 作答提要
 - 基本上，答案都在提示提及了。
 - 其餘按程式撰寫邏輯就能順利完成了。
 - 打開網頁檔
 - 右鍵選單 > "**檢查**"
 - 打開的右半邊視窗內，ctrl + F 搜尋 ".contents_box02"
 - 依題意找到 "**大樂透**" 的標籤區塊。
 - 標籤區塊右鍵選單 > "Scroll into view"，左邊就會自動帶出題意的那些號碼。
 - 按題意依序填答。

   
## 特別注意
 - 預覽一下變數 balls 的內容，變數 i 不過是純整數。
 - 若僅要文字內容，用屬性 text。
 - find_all 會回傳一個 list。
