## 作答提要
 - 基本上，答案都在提示提及了。
 - 其餘按程式撰寫邏輯就能順利完成了。
 - 中後段？打開網頁檔，則一操作：
   1. 直接往下找到 "NTD/USD"
   2. ctrl + F 搜尋 "NTD/USD"
 - 右鍵選單 > "**檢查**"，往上層級找到首個有 class 的標籤。
 - 按題意依序填答。
 - 綜合變數名稱可以發現，要找的就是該標籤下所有的 tr 標籤。
   
## 特別注意
 > 末兩空格若有疑，觀察標籤 tr 下的標籤名稱。
