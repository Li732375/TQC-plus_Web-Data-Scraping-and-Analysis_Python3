## 作答提要
 - 基本上，答案都在提示提及了。
 - 其餘按程式撰寫邏輯就能順利完成了。
   
## 特別注意
 - 套件 sqlite3 的 "**讀檔方法**" 為 **connect**（看字義也能猜到，資料庫是用 **連接** 的）。
 > 沒用過資料庫？不怕，把它簡單理解為倉管，所以任何要從裡面取出存入的資料，得配合倉管的 "**指定語法**"。

 - 套件 sqlite3 "**實例化**"（設置該物件的初始狀態） 的方法為 **cursor**。

 - 套件 sqlite3 實例的方法 "**執行語法**" 為 **execute**，參數為 SQL語法（字串）。
 > 補充：字串的內容就是 "**指定語法**" 其中功能 --- **查詢** （從讀入資料庫內，找尋 表格名稱 "Employee"，取其內容 所有 "*"）。

 - 套件 sqlite3 實例的 "**取得查詢結果**" 方法為 **fetchall**。

