## 作答提要
 - 四角落元素，採用似"篩選"的方法，屬於套件 numpy 下，名稱為 ix_，參數為兩個 list，由左而右：
   1. 長度同目標陣列維度值的 list，元素表示在該維度的 "**行**" 索引。
   2. 長度同目標陣列維度值的 list，元素表示在該維度的 "**列**" 索引。
 > 因此依題意，目標陣列形狀為 3[0~2] x 5[0~4]，取得第 0 和第 2 行、第 0 和第 4 列索引條件陣列。

## 特別注意
 - 最大最小總和，皆採用各自類別方法！